#!/usr/bin/node
// prints 3 lines using an array of string and a loop

// define array of 3 lines to print
const arr = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

// loop through array, print each line
for (let i = 0; i < arr.length; i++) {
  console.log(arr[i]);
}
